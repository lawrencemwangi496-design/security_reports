# OpenClaw Security Report — 2026-05-28

## Verdict: PASS

## Summary
The executed scanners (npm audit and Trivy dependency/secret/misconfig checks) reported zero vulnerabilities, secrets, or Dockerfile misconfigurations. However, the Snyk scan did not run due to a 401 authentication error, so coverage is incomplete and there is some residual risk until Snyk results are obtained.

## Critical Findings


## Warnings
- Snyk scan failed with SNYK-0005 (401 Unauthorized), so Snyk vulnerability coverage is missing for this run

## Recommendation
Deployment is not blocked based on current evidence because no HIGH/CRITICAL vulnerabilities with available fixes were detected by the scans that completed. That said, this should only proceed if your release policy allows deployment with a missing scanner; otherwise treat this as a gating failure and rerun after restoring Snyk access. Fix Snyk authentication by running `snyk auth` (interactive) or configuring a CI secret `SNYK_TOKEN` for the build environment, then rerun `snyk test` (and `snyk monitor` if you use continuous tracking) to ensure the dependency graph is fully assessed by all required tools before considering this scan set complete.
