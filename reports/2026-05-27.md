# OpenClaw Security Report — 2026-05-27

## Verdict: PASS

## Summary
NPM Audit and Trivy dependency scans report zero vulnerabilities across the scanned lockfiles and manifests, and the Dockerfile misconfiguration scan is clean. The only issue observed is that Snyk failed to run due to missing/invalid authentication, so coverage is incomplete even though current scan outputs are clean.

## Critical Findings


## Warnings
- Snyk scan did not execute (401 Unauthorized / SNYK-0005), which reduces confidence in the overall assessment because Snyk-specific vulnerability and license checks were not performed.

## Recommendation
Deploy is acceptable from a vulnerability/misconfiguration perspective based on the completed scans (0 vulns, 0 secrets, 0 Dockerfile misconfigs), but treat this as a provisional PASS because one configured scanner (Snyk) failed and therefore did not contribute results. Next: restore Snyk coverage by authenticating in CI (run `snyk auth` locally, or configure a CI secret such as `SNYK_TOKEN` and rerun the Snyk job), then rerun the full security pipeline to confirm Snyk also reports clean before relying on this as a release gate; if Snyk later reports HIGH/CRITICAL with fixes, block deployment until patched.
