# OpenClaw Security Report — 2026-05-23

## Verdict: PASS

## Summary
The provided scans show no HIGH or CRITICAL vulnerabilities, and Trivy reports no dependency, secrets, or Dockerfile misconfiguration findings. The only issue is a single MODERATE npm vulnerability in qs with an available automated fix. Overall posture looks good with a small dependency hygiene item to address.

## Critical Findings


## Warnings
- npm audit: qs 6.11.1 - 6.15.1 has a remotely triggerable DoS in qs.stringify when encodeValuesOnly is set (moderate); fix available via `npm audit fix`
- Snyk scan did not run due to authentication error (401 Unauthorized, SNYK-0005), reducing coverage until credentials are configured

## Recommendation
This should not be blocked on security grounds because there are no HIGH/CRITICAL findings, but you should remediate the moderate qs issue before release by running `npm audit fix` (or explicitly upgrading qs to a patched version if audit fix does not update it) and re-running `npm audit` to confirm it is cleared. Additionally, restore full SCA coverage by authenticating Snyk (`snyk auth`) or provisioning access so future scans don’t silently miss issues; treat the current Snyk 401 as a pipeline health problem (coverage gap) and add a CI check that fails the job when scanners cannot authenticate or execute.
